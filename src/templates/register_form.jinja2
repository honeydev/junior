{% extends 'base.jinja2' %}
{% block header %}
{% endblock %}
{% block content %}
<div class="container">
  <div class="row justify-content-center">
    <div class="card mt-4 col-lg-6 col-11">
      <div class="row card-header">
        <div class="mx-auto col-auto">РЕГИСТРАЦИЯ</div>
      </div>
      
      {% include 'components/form_error.jinja2' %}

      <form class="form-horizontal" action="{{ url_for('auth.registration') }}" method="post">
      {{ form.csrf_token }}
        <div class="form-group">

          {% for form in form %}
            <div class="row">
              <div class="mx-auto col-10 mt-3">
                {{ form(class_='form-control', placeholder = form.label.text) }}
              </div>
            </div>
          {% endfor %}

          <div class="row">
            <div class="mx-auto col-auto mt-4">
              <button type="submit" class="btn btn-success">Зарегистрироваться</button>
            </div>
          </div>
        </div>
      </form>
      </div>
  </div>
  {% include 'components/or.jinja2' %}
  {% include 'components/social_login.jinja2' %} 
</div>
{% endblock %}
